# Required compatibility update

The requested integration requires `api_level=2`.

After changing the configuration, run:

```bash
python3 build.py
```

Do not keep the update if the required build fails.
